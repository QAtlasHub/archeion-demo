# The logistic map, as a model record

- Record `r_4aehb2y5`, revision `20260915T071940Z-3ve4` (the first).
- Project: demo (`p_z7ne42dt`). Status: final. Tags: example, chaos.
- Frozen 2026-09-15T07:19:40Z.

Open `gallery/index.html` for the report. `agent/agent.json` is the same report for a program:
each figure as the table of what it plots.

## Where it came from

Rendered by `scripts/build.jl` at commit `340c965b8c3a8fab38857671a410ecfe13107c31` of
https://github.com/QAtlasHub/archeion-demo.git, clean. The git state was read when the record was
deposited, not when it was rendered.

## What it can be trusted for

Read only. The gallery opens from this directory; its equations need KaTeX from cdn.jsdelivr.net.
The render environment was not captured, so the report cannot be rebuilt from what is here.

## History

Converted from the Archeion 0.3 layout (`demo/logistic`). Archeion's bookkeeping files and its
`reproduce.sh`, which could not have run, were not carried over.
