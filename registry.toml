# What this registry is, for a reader that finds it without its tools.
spec = "registry/1"                       # the format: SPEC.md in QAtlasHub/Archeion.jl
name = "archeion-demo"
implementation = "Archeion.jl v0.8.1"     # the version CI validates and builds with
